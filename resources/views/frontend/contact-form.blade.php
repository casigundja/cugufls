<form method="post" action="/contacto">@csrf<input type="hidden" name="type" value="{{ $type ?? 'general' }}">@if(!empty($segmentId))<input type="hidden" name="segment_id" value="{{ $segmentId }}">@endif
@if(!empty($branchId))<input type="hidden" name="branch_id" value="{{ $branchId }}">@endif<div style="display:none" aria-hidden="true"><label>Website<input name="website" tabindex="-1" autocomplete="off"></label></div><div class="form-grid"><div class="field"><label for="contact_name">Nome</label><input id="contact_name" name="name" value="{{ old('name') }}" required minlength="2" maxlength="150" autocomplete="name"></div><div class="field"><label for="contact_phone">Telefone</label><input id="contact_phone" name="phone" value="{{ old('phone') }}" type="tel" autocomplete="tel"></div><div class="field"><label for="contact_email">Email</label><input id="contact_email" name="email" type="email" value="{{ old('email') }}" autocomplete="email"></div><div class="field"><label for="company">Empresa (opcional)</label><input id="company" name="company" value="{{ old('company') }}" maxlength="150"></div><div class="field full"><label for="message">Como podemos ajudar?</label><textarea id="message" name="message" required minlength="10" maxlength="3000">{{ old('message') }}</textarea></div></div><button class="button">Enviar mensagem ↗</button><p class="muted" style="margin-top:15px">Usaremos os seus contactos para responder a esta solicitação. Não envie informações clínicas ou receitas neste formulário.</p></form>
