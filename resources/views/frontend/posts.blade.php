@extends('layouts.public')
@section('content')<section class="page-hero"><div class="container"><span class="eyebrow">ACONTECE NA FAMÍLIA</span><h1>Novidades.</h1></div></section><section class="container section">@if($posts->isEmpty())<div class="empty-state"><h3>Em breve, novidades por aqui.</h3><p>Acompanhe os nossos produtos, serviços e notícias.</p></div>@else<div class="news-grid">@foreach($posts as $post)<a class="news-card" href="/novidades/{{ $post->slug }}">@if($post->image)<img src="{{ Storage::disk('public')->url($post->image) }}" alt="">@endif<small>{{ $post->published_at->format('d.m.Y') }}</small><h3>{{ $post->title }}</h3><p>{{ $post->excerpt }}</p></a>@endforeach</div>{{ $posts->links() }}@endif</section>@endsection
