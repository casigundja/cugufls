<article class="product-card"><a href="/produtos/{{ $product->slug }}" class="product-image">@if($product->productImages->isNotEmpty())<img src="{{ Storage::disk('public')->url($product->productImages->sortBy('sort_order')->first()->path) }}" alt="{{ $product->name }}" loading="lazy">@else<span class="product-placeholder">g<span>+</span></span>@endif</a><small>{{ $product->segment->name }}</small><h3><a href="/produtos/{{ $product->slug }}">{{ $product->name }}</a></h3><div class="product-bottom"><strong>{{ $product->price !== null ? number_format((float)($product->sale_price ?? $product->price), 2, ',', '.').' Kz' : 'Sob consulta' }}</strong><a href="/produtos/{{ $product->slug }}" aria-label="Ver {{ $product->name }}">↗</a></div></article>
